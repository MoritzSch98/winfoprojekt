package com.example.gewerbeanmeldung.Choices;

import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping(path = "/choice")
public class ChoicesController {

	/*
	 * @RequestMapping(path = "/{id}/choices") public List<Choices>
	 * getAllChoices(@PathVariable("id") Integer question_id) { return
	 * choicesService.getAllChoices(question_id); }
	 * 
	 * 
	 * @Autowired FrageartService frageService;
	 * 
	 * 
	 * //Lists all Questions
	 * 
	 * @RequestMapping(path = "") public List<Frageart> getAllFragen() { return
	 * frageService.getAllFragen(); }
	 * 
	 * //Get a Question by its ID
	 * 
	 * @RequestMapping(path="/{id}") public Frageart
	 * getFrageById(@PathVariable("id") Integer id) { return
	 * frageService.getFrageById(id); }
	 * 
	 * //Add a new Question
	 * 
	 * @RequestMapping( method = RequestMethod.POST,value = "") public Frageart
	 * saveFrage(@RequestBody Frageart frage) { return
	 * frageService.saveFrage(frage); }
	 * 
	 * //Delete a Question by ID
	 * 
	 * @RequestMapping(method = RequestMethod.DELETE,value = "/{id}") public String
	 * deleteFrage(@PathVariable("id") Integer id) { frageService.deleteFrage(id);
	 * return "deleted successful"; }
	 * 
	 * //Edit Question by Id
	 * 
	 * @RequestMapping(method = RequestMethod.PUT,value = "{id}") public String
	 * updateFrage(@RequestBody Frageart frage, @PathVariable("id") Integer id) {
	 * frageService.updateFrage(frage, id); return "edited successful"; }
	 */
}
